{
  "title": "Matching Engine Hot Path",
  "schemaVersion": 39,
  "panels": [
    {
      "type": "graph",
      "title": "CPU throttled",
      "targets": [
        { "expr": "sum(rate(container_cpu_cfs_throttled_seconds_total{pod=~\"matching.*\"}[1m]))" }
      ]
    }
  ]
}
